let color = {
    color: yellow,
    color: blue,
    color: orange,
    color: red,
};

person.color = yellow, blue, orange, red;
console.log (person.color)

while (color) {
    color++
    return color
}


for (color = green; color = yellow, blue, orange, red; color++) {
    Array.length 
}



for each (color) {
    Array.prototype.forEach(color)
}